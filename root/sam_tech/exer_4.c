#include <stdio.h>
/**
 * Write a program that can print out the numeric value 
 * of the newline character (\n). (Hint: assign ‘\n’ to a character variable.)
 */

int main()
{
	char newline;
	newline = '\n';

	printf("The numeric value of newline character is: %d", newline);
	return (0);
}
