#include <stdio.h>
/**
 * A program that prints the string "+School" 4 times
 */

int main(void)
{
	char name[] = "+School";
	int i;

	for(i = 0; i < 4; i++);
	{
		printf("%s\n", name);
	}
	return (0);
}

